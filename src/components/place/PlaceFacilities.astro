---
import Badge from '@/components/base/Badge.astro';
import type { Place } from '@/types/place';
const { place } = Astro.props as { place: Place };
---
<section class="border border-ink bg-surface p-5 shadow-offset">
  <h2 class="display-heading text-3xl">Fasilitas</h2>
  <div class="mt-4 flex flex-wrap gap-2">
    {place.facilities.length ? place.facilities.map((term) => <Badge icon={term.icon}>{term.name}</Badge>) : <p class="text-sm text-muted">Fasilitas belum dicatat.</p>}
  </div>
</section>
