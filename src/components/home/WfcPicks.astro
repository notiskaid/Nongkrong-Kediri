---
import PlaceListingGrid from '@/components/listing/PlaceListingGrid.astro';
import type { Place } from '@/types/place';
const { places = [] } = Astro.props as { places: Place[] };
---
<section>
  <div class="mb-4">
    <h2 class="text-2xl font-semibold">Rekomendasi WFC</h2>
    <p class="mt-2 text-sm text-muted">Tempat dengan use case WFC dan fasilitas WiFi/colokan.</p>
  </div>
  <PlaceListingGrid places={places} />
</section>
